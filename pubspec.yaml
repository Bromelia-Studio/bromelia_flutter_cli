name: bromelia_cli
description: Bromelia CLI is a command-line tool for generating Flutter projects.
version: 1.0.1
repository: https://github.com/Bromelia-Studio/bromelia_flutter_cli

environment:
  sdk: ^3.9.0

dependencies:
  args: ^2.7.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3

executables:
  bromelia_cli: bromelia_cli

assets:
   - templates/flutter_app/
